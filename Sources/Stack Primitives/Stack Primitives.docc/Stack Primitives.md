# ``Stack_Primitives``

@Metadata {
    @DisplayName("Stack Primitives")
    @TitleHeading("Swift Primitives")
}

A dynamically-growing LIFO stack for move-only and copyable elements — O(1) amortized push and pop, with a fixed-capacity `Stack.Bounded` variant for allocation-free use.

## Topics
