# .github/metadata.yaml
# Triaged by AI-assisted bulk pass (2026-04-29).
description: "Stack — a dynamically-growing LIFO stack for move-only and copyable elements (O(1) amortized push/pop), with a fixed-capacity Stack.Bounded variant."
topics:
  - primitives
  - stack
homepage: "https://swift-institute.org"
readme:
  family: E
